VantageLife 2.1 (AO Premiere) — AI context pointer

This file used to be a snapshot bundle of CLAUDE.md plus copies of source
files. Those snapshots drifted badly from the real app (they described a
Supabase/Node stack that never existed in this repo), so the bundle was
retired during the 2026-07 repo consolidation.

Canonical, maintained context now lives in:

- CLAUDE.md            — stack, architecture, business rules, RBAC, the 14 metrics
- README.md            — how to run, build, and test
- memory/PRD.md        — product requirements
- auth_testing.md      — auth/RBAC testing playbook

The app itself: Expo (SDK 56) / React Native frontend under frontend/
(Expo Router), Python/FastAPI backend in backend/server.py over MongoDB,
deployed on Railway (backend) and Vercel (web export). Do not paste stale
code snapshots into this file — link to the source instead.
