modules = ["python-3.11"]
run = "cd backend && pip install -r requirements.txt && uvicorn server:app --host 0.0.0.0 --port 8000"

[deployment]
run = ["sh", "-c", "cd backend && pip install -r requirements.txt && uvicorn server:app --host 0.0.0.0 --port $PORT"]
